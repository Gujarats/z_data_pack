# Keep Witcher 2 XML files UTF-16 in the working tree, but store them as UTF-8
# in Git so GitHub can render diffs instead of classifying them as binary.
*.xml text working-tree-encoding=UTF-16 eol=crlf
